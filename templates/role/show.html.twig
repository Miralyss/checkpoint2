{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('role_index') }}" class="btn btn-info">Retour</a>

    <a href="{{ path('role_edit', {'id': role.id}) }}" class="btn btn-info">Editer</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
